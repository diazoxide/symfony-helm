apiVersion: v1
kind: Service
metadata:
  name: {{ include "diazoxide-symfony.fullname" . }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  labels:
    {{- include "diazoxide-symfony.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "diazoxide-symfony.selectorLabels" . | nindent 4 }}
